[package]
name = "comboforge-bridge-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Message types for the ComboForge Bridge loopback protocol. No I/O, no unsafe."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
